import Link from "next/link";
import Image from "next/image";

export default function OngoingPage({ seriesData, total, totalPage, page }) {
  return {
<>
    <header>
        Header
    </header>
    <main>
        main
    </main>
      <section>
        Navigasi
      </section>
</>
  };
}
